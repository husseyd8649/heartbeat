import { statusSnapshot } from '@/lib/server/repository'
export const dynamic='force-dynamic'
export default async function PublicStatusPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const data=await statusSnapshot(slug);return <main style={{maxWidth:880,margin:'60px auto',padding:24,fontFamily:'system-ui'}}><p style={{textTransform:'uppercase',letterSpacing:'.15em',fontSize:11,color:'#b85b3c'}}>Public status</p><h1>{slug} service health</h1><p style={{color:'#666'}}>Published from the same persisted checks that power alerts.</p><div style={{margin:'28px 0',padding:20,borderRadius:14,background:data.overall==='degraded'?'#fff0ec':'#e7f7ef'}}><b>{data.overall==='degraded'?'One service is investigating an issue':'All systems operational'}</b><p>{data.overall==='degraded'?'Recovery is being monitored.':'No active incidents.'}</p></div>{data.monitors.map(m=><div key={m.id} style={{display:'flex',justifyContent:'space-between',padding:'16px 0',borderBottom:'1px solid #eee'}}><div><b>{m.name}</b><small>{m.target}</small></div><b style={{color:m.currentState==='healthy'?'#19754c':'#bb5137'}}>{m.currentState==='healthy'?'Operational':'Investigating'}</b></div>)}</main>}
